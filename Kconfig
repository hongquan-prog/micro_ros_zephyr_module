# PWM/DDS demo application Kconfig.
#
# When an application provides its own Kconfig it becomes the Kconfig root,
# so the Zephyr core tree must be sourced explicitly.

mainmenu "PWM/DDS demo"

source "Kconfig.zephyr"

menu "PWM/DDS demo"

choice DEMO_HEARTBEAT_BACKEND
	prompt "Heartbeat backend"
	default DEMO_HEARTBEAT_STUB

config DEMO_HEARTBEAT_STUB
	bool "Stub (Phase 1: simulated Linux round trip)"

config DEMO_HEARTBEAT_MICROROS
	bool "micro-ROS (Phase 2: real DDS link)"
	select MICROROS

endchoice

config DEMO_DDS_SHELL
	bool "DDS diagnostic shell (dds status/stats/shm)"
	default y
	help
	  Enable the "dds" shell command set for diagnosing the heartbeat
	  link: session/entity/slot status, counters and shmem slot dumps.
	  Requires CONFIG_SHELL=y (see prj.conf).

endmenu
